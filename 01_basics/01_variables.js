// types of variables const , let , var
// you cannot declare two variables the same id 

const accountId = "1"
// to declare a name or any word it should be in a  "" as a string 
// in const the value can never be changed in  code

let accountPassword = "1" 
// we need to define let with ( " " ) or it wont be displayed


console.table([accountId , accountPassword]);
// two types of console .log and . table where table converts all the console id into table and print it 