const btn = document.querySelectorAll(".button")
const bdy = document.querySelector("body")

// btn.forEach(function(btns){  // here for eahc btn( btn is all button as we stated in above variable)
//    console.log(btn);          // if console is ritten inside the nwe have given a loop for a btn iside the function
// })
 


// this is done using loop 

// btn.forEach(function(btns){
//     //console.log(btns);

//     btns.addEventListener('click', function (nameanything) {    // in event we have to carry a object which has all the evnts and we have to call that
//         if (nameanything.target.id === "grey") {
//             bdy.style.backgroundColor = nameanything.target.id
//         }
//         if (nameanything.target.id === "white") {
//             bdy.style.backgroundColor = nameanything.target.id
//         }
//         if (nameanything.target.id === "blue") {
//             bdy.style.backgroundColor = nameanything.target.id
//         }
//         if (nameanything.target.id === "yellow") {
//             bdy.style.backgroundColor = nameanything.target.id
//         }
//     })
// })
// // event.target kis element se ye target aaya hai 

// now using switch case

